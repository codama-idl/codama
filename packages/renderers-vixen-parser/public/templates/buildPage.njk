{% extends "layout.njk" %}

{% block main %}

fn main() {
    // #[cfg(feature = "proto")]
    prost_build::Config::new()
        .enable_type_names()
        .compile_protos(&["proto/proto_def.proto"], &["proto"])
        .unwrap();
}

{% endblock %}